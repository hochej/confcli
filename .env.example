# Example environment configuration for confcli
#
# IMPORTANT:
# - Do NOT commit real credentials.
# - By default, confcli will NOT auto-load a .env file in release builds.
#   To opt in, set CONFCLI_LOAD_DOTENV=1 when running.
#
# Confluence site URL (Cloud instances typically include /wiki)
CONFLUENCE_DOMAIN=example.atlassian.net

# Basic auth (email + API token)
CONFLUENCE_EMAIL=you@example.com
CONFLUENCE_TOKEN=your_api_token_here
